const {
  createDatabase,
  createEventsCollection,
  seed,
  createBucket,
} = require("./appwrite-server");
const events = require("./events.json");

// createDatabase();
// createEventsCollection();
// createBucket();
// seed(events);
